---
import settings from '../data/settings.json';
import { withBase } from '../lib/paths';
import logoUrl from '../assets/logo.svg?url';

const nav = [
  { href: withBase('/leistungen/'), label: 'Leistungen' },
  { href: withBase('/referenzen/'), label: 'Referenzen' },
  { href: withBase('/ueber-uns/'), label: 'Über uns' },
  { href: withBase('/kontakt/'), label: 'Kontakt' }
];
const path = Astro.url.pathname;
---
<header class="site-header">
  <div class="header-shell">
    <a class="brand" href={withBase('/')} aria-label={`${settings.marke} – Startseite`}>
      <img class="brand-mark" src={logoUrl} width="449" height="452" alt="" />
      <span class="brand-copy">
        <span class="brand-name">{settings.marke}</span>
        <span class="brand-claim">{settings.claim}</span>
      </span>
    </a>

    <nav class="site-nav" aria-label="Hauptnavigation" id="site-nav">
      <div class="nav-links">
        {nav.map((item) => (
          <a href={item.href} aria-current={path.startsWith(item.href) ? 'page' : undefined}>
            {item.label}
          </a>
        ))}
      </div>

      <div class="nav-actions">
        <a class="nav-phone" href={`tel:${settings.telefonLink}`} aria-label={`Anrufen: ${settings.telefon}`}>
          <span class="nav-phone-line" aria-hidden="true"></span>
          <span>{settings.telefon}</span>
        </a>

        <a class="nav-cta" href={withBase('/kontakt/')}>
          <span>Projekt anfragen</span>
          <svg aria-hidden="true" viewBox="0 0 18 18">
            <path d="M5 13 13 5M7 5h6v6" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5"/>
          </svg>
        </a>
      </div>
    </nav>

    <button class="menu-btn" type="button" aria-expanded="false" aria-controls="site-nav" aria-label="Menü öffnen">
      <span></span><span></span>
    </button>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 100;
    color: var(--ink);
    background: rgba(252, 250, 246, .985);
    border-top: 3px solid #b9822f;
    border-bottom: 1px solid rgba(23, 24, 27, .10);
    transition: background-color 180ms ease, box-shadow 180ms ease;
  }

  .site-header.is-scrolled {
    background: rgba(252, 250, 246, .995);
    box-shadow: 0 .65rem 2rem rgba(23, 24, 27, .06);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
  }

  .header-shell {
    display: grid;
    width: min(calc(100% - clamp(2rem, 5vw, 5rem)), 92rem);
    min-height: 5.6rem;
    margin-inline: auto;
    grid-template-columns: minmax(16rem, 1fr) auto minmax(13rem, 1fr);
    align-items: center;
    gap: clamp(1.75rem, 3vw, 3.5rem);
  }

  .brand {
    display: inline-flex;
    width: fit-content;
    align-items: center;
    gap: .9rem;
    color: var(--ink);
    text-decoration: none;
  }

  .brand-mark {
    width: 3.3rem;
    height: 3.3rem;
    flex: 0 0 auto;
    object-fit: contain;
    opacity: .94;
    transition: opacity 170ms ease, transform 220ms var(--ease-move);
  }

  .brand-copy {
    display: grid;
    gap: .26rem;
    min-width: 0;
  }

  .brand-name {
    color: #16171a;
    font-family: var(--font-brand);
    font-size: 1.34rem;
    font-weight: 700;
    letter-spacing: -.035em;
    line-height: .98;
  }

  .brand-claim {
    color: #7d5518;
    font-family: var(--font-text);
    font-size: .66rem;
    font-weight: 700;
    letter-spacing: .12em;
    line-height: 1.2;
    text-transform: uppercase;
  }

  .site-nav {
    display: contents;
  }

  .nav-links {
    grid-column: 2;
    display: flex;
    min-height: 5.6rem;
    align-items: center;
    justify-content: center;
    gap: clamp(1.65rem, 2.35vw, 2.65rem);
  }

  .nav-links > a {
    position: relative;
    display: inline-flex;
    min-height: 5.6rem;
    align-items: center;
    color: #25272b;
    font-family: var(--font-text);
    font-size: 1.02rem;
    font-weight: 640;
    letter-spacing: -.012em;
    text-decoration: none;
    white-space: nowrap;
    transition: color 160ms ease;
  }

  .nav-links > a::after {
    position: absolute;
    right: 50%;
    bottom: 1.15rem;
    left: 50%;
    height: 2px;
    background: #b9822f;
    content: '';
    transition: right 190ms var(--ease-move), left 190ms var(--ease-move);
  }

  .nav-links > a[aria-current="page"] {
    color: #111214;
  }

  .nav-links > a[aria-current="page"]::after {
    right: 0;
    left: 0;
  }

  .nav-actions {
    grid-column: 3;
    display: flex;
    align-items: center;
    justify-content: flex-end;
  }

  .nav-phone {
    display: none;
  }

  .nav-cta {
    display: inline-flex;
    min-height: 3.25rem;
    align-items: center;
    justify-content: center;
    gap: .8rem;
    padding: .86rem 1.28rem .86rem 1.4rem;
    color: #fff;
    background: #1a1b1e;
    border: 1px solid #1a1b1e;
    font-family: var(--font-text);
    font-size: .98rem;
    font-weight: 680;
    letter-spacing: -.012em;
    text-decoration: none;
    white-space: nowrap;
    transition: color 170ms ease, background-color 170ms ease, border-color 170ms ease, transform 170ms var(--ease-move);
  }

  .nav-cta svg {
    width: 1rem;
    height: 1rem;
    color: #d7b36b;
    transition: transform 200ms var(--ease-move);
  }

  .menu-btn { display: none; }

  @media (hover: hover) and (pointer: fine) {
    .brand:hover .brand-mark { opacity: 1; transform: scale(1.02); }
    .nav-links > a:hover { color: #111214; }
    .nav-links > a:hover::after { right: 0; left: 0; }
    .nav-cta:hover {
      color: #17181b;
      background: #d7b36b;
      border-color: #d7b36b;
      transform: translateY(-1px);
    }
    .nav-cta:hover svg { color: #17181b; transform: translate(.12rem, -.12rem); }
  }

  @media (max-width: 76rem) {
    .header-shell {
      width: min(calc(100% - 2.5rem), 88rem);
      grid-template-columns: minmax(13rem, 1fr) auto minmax(10rem, 1fr);
      gap: 1.5rem;
    }

    .brand-mark { width: 3rem; height: 3rem; }
    .brand-name { font-size: 1.2rem; }
    .brand-claim { font-size: .61rem; }
    .nav-links { gap: 1.3rem; }
    .nav-links > a { font-size: .95rem; }
    .nav-cta { padding-inline: 1.05rem 1.15rem; font-size: .92rem; }
  }

  @media (max-width: 64rem) {
    .header-shell {
      grid-template-columns: minmax(12rem, 1fr) auto auto;
      gap: 1.1rem;
    }
    .nav-links { gap: 1rem; }
    .nav-links > a { font-size: .9rem; }
  }

  @media (max-width: 56rem) {
    .site-header { min-height: 4.75rem; border-top-width: 2px; }

    .header-shell {
      display: flex;
      width: calc(100% - 2rem);
      min-height: 4.75rem;
      gap: 1rem;
    }

    .brand { gap: .7rem; }
    .brand-mark { width: 2.65rem; height: 2.65rem; }
    .brand-name { font-size: 1.08rem; }
    .brand-claim { font-size: .56rem; letter-spacing: .1em; }

    .site-nav {
      position: fixed;
      top: calc(4.75rem + 2px);
      right: 0;
      bottom: 0;
      left: 0;
      z-index: 3;
      display: grid;
      grid-template-rows: 1fr auto;
      gap: 2rem;
      padding: clamp(2rem, 7vw, 3rem) var(--pad-inline) calc(1.6rem + env(safe-area-inset-bottom));
      overflow-y: auto;
      color: var(--ink);
      background: #fbf9f4;
      opacity: 0;
      visibility: hidden;
      transform: translateY(-.4rem);
      transition: opacity 200ms ease, transform 240ms var(--ease-move), visibility 0s linear 240ms;
    }

    .site-nav.open {
      opacity: 1;
      visibility: visible;
      transform: none;
      transition-delay: 0s;
    }

    .nav-links {
      display: grid;
      min-height: 0;
      align-content: start;
      gap: 0;
    }

    .nav-links > a {
      min-height: 4.85rem;
      color: #17181b;
      border-bottom: 1px solid rgba(28,29,31,.13);
      font-family: var(--font-brand);
      font-size: clamp(1.55rem, 6vw, 2rem);
      font-weight: 650;
      letter-spacing: -.03em;
    }

    .nav-links > a:first-child { border-top: 1px solid rgba(28,29,31,.13); }
    .nav-links > a::after {
      right: auto;
      bottom: 0;
      left: 0;
      width: 0;
      height: 2px;
      transition: width 190ms var(--ease-move);
    }
    .nav-links > a[aria-current="page"]::after { width: 3.2rem; }

    .nav-actions {
      display: grid;
      gap: 1rem;
    }

    .nav-phone {
      display: inline-flex;
      min-height: 3.2rem;
      align-items: center;
      gap: .7rem;
      color: #17181b;
      font-size: 1rem;
      font-weight: 650;
      text-decoration: none;
    }

    .nav-phone-line {
      width: 2rem;
      height: 1px;
      background: #b9822f;
    }

    .nav-cta {
      min-height: 3.9rem;
      justify-content: space-between;
      font-size: 1rem;
    }

    .menu-btn {
      display: grid;
      width: 2.9rem;
      height: 2.9rem;
      margin-left: auto;
      padding: 0;
      place-content: center;
      gap: 7px;
      border: 1px solid rgba(23,24,27,.16);
      color: #17181b;
      background: transparent;
      cursor: pointer;
      -webkit-tap-highlight-color: transparent;
    }

    .menu-btn span {
      width: 1.35rem;
      height: 1px;
      background: currentColor;
      transform-origin: center;
      transition: transform 210ms var(--ease-move);
    }

    .menu-btn[aria-expanded="true"] span:first-child { transform: translateY(4px) rotate(45deg); }
    .menu-btn[aria-expanded="true"] span:last-child { transform: translateY(-4px) rotate(-45deg); }
  }

  @media (max-width: 30rem) {
    .brand-claim { display: none; }
  }

  @media (prefers-reduced-motion: reduce) {
    .site-header,
    .brand-mark,
    .nav-links > a::after,
    .nav-cta,
    .nav-cta svg,
    .site-nav,
    .menu-btn span { transition: none !important; }
  }
</style>

<script>
  const header = document.querySelector('.site-header');
  const button = header?.querySelector('.menu-btn');
  const navigation = header?.querySelector('#site-nav');
  const mobile = window.matchMedia('(max-width: 56rem)');

  const setMenu = (open) => {
    navigation?.classList.toggle('open', open);
    header?.classList.toggle('menu-open', open);
    button?.setAttribute('aria-expanded', String(open));
    button?.setAttribute('aria-label', open ? 'Menü schließen' : 'Menü öffnen');
    document.body.classList.toggle('nav-open', open && mobile.matches);
  };

  const setScrolled = () => header?.classList.toggle('is-scrolled', window.scrollY > 12);

  button?.addEventListener('click', () => setMenu(button.getAttribute('aria-expanded') !== 'true'));
  navigation?.querySelectorAll('a').forEach((link) => link.addEventListener('click', () => setMenu(false)));
  document.addEventListener('keydown', (event) => {
    if (event.key === 'Escape' && button?.getAttribute('aria-expanded') === 'true') {
      setMenu(false);
      button.focus();
    }
  });
  mobile.addEventListener('change', (event) => {
    if (!event.matches) setMenu(false);
  });
  window.addEventListener('scroll', setScrolled, { passive: true });
  setScrolled();
</script>

<style is:global>
  @media (max-width: 56rem) {
    body.nav-open { overflow: hidden; }
  }
</style>
